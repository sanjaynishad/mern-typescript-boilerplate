import { Component, ReactNode } from "react";

export default class AdminDashboardPage extends Component {
    render(): ReactNode {
        return (
            <>
                Admin dashboard
            </>
        );
    }
}
